# ota:managed-github-projection v1 identity=sha256:5c83ae6f8b86ea06f28ecb31525cbc48105b307529d6a34b1f955d01dd0102f9
name: Ota governance ('verify-static')

on:
  workflow_call:
    inputs:
      ota_projection_identity:
        description: Ota-managed projection identity
        required: true
        type: string
      ota_runner:
        description: GitHub runner selected by the human-owned caller
        required: false
        default: 'ubuntu-latest'
        type: string
      ota_target_os:
        description: Target operating system bound into the Ota projection
        required: true
        type: string

jobs:
  ota_ota_verify_verify_static:
    name: 'ota.verify.verify-static (linux/native)'
    runs-on: ${{ inputs.ota_runner }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: ota-run/setup@493cba84bf7f7c11c9e8e996d832d93a89c62184
        with:
          source: contract
      - uses: ruby/setup-ruby@003a5c4d8d6321bd302e38f6f0ec593f77f06600
        with:
          ruby-version: '3.3.11'
      - name: Verify Ota projection identity
        run: ota ci projection --workflow 'verify-static' --mode native --target-os ${{ inputs.ota_target_os }} --expect-identity ${{ inputs.ota_projection_identity }} --json
      - name: Validate contract
        run: ota validate . --json
      - name: Diagnose contract lane
        run: ota doctor --workflow 'verify-static' --mode native --json
      - name: Discover safe execution surface
        run: ota tasks --safe --use --json
      - name: Preview contract lane
        run: ota up --workflow 'verify-static' --mode native --agent --dry-run --json
      - name: Run contract lane
        run: ota up --workflow 'verify-static' --mode native --agent --json
      - name: Archive contract receipt
        run: ota receipt --workflow 'verify-static' --mode native --archive --json
  ota_ota_refusal_canary_task_verify:
    name: 'ota.refusal-canary.task.verify (linux/native)'
    runs-on: ${{ inputs.ota_runner }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: ota-run/setup@493cba84bf7f7c11c9e8e996d832d93a89c62184
        with:
          source: contract
      - uses: ruby/setup-ruby@003a5c4d8d6321bd302e38f6f0ec593f77f06600
        with:
          ruby-version: '3.3.11'
      - name: Verify Ota projection identity
        run: ota ci projection --workflow 'verify-static' --mode native --target-os ${{ inputs.ota_target_os }} --expect-identity ${{ inputs.ota_projection_identity }} --json
      - name: Prove agent refusal canary
        run: ota run --agent --expect-refusal --mode native --json 'verify'
  ota_ota_refusal_canary_workflow_app:
    name: 'ota.refusal-canary.workflow.app (linux/native)'
    runs-on: ${{ inputs.ota_runner }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: ota-run/setup@493cba84bf7f7c11c9e8e996d832d93a89c62184
        with:
          source: contract
      - uses: ruby/setup-ruby@003a5c4d8d6321bd302e38f6f0ec593f77f06600
        with:
          ruby-version: '3.3.11'
      - name: Verify Ota projection identity
        run: ota ci projection --workflow 'verify-static' --mode native --target-os ${{ inputs.ota_target_os }} --expect-identity ${{ inputs.ota_projection_identity }} --json
      - name: Prove agent refusal canary
        run: ota up --agent --expect-refusal --workflow 'app' --mode native --json
